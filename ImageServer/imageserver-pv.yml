apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: imageserver-pv-claim
spec:
  storageClassName: do-block-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
